#include <Servo.h>
#include "math.h"
void setup() {
  // put your setup code here, to run once:

}

void loop() {
  // put your main code here, to run repeatedly:
  double x;
  double y;
  double uplen = 30;
  double lowlen = 30;
  double handlen = 10;
  double r = sqrt(sq(x) + sq(y));
  double baseang = atan(x/y);
  
}
